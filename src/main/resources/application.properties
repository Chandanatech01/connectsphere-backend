spring.application.name=scm2.0

# Server — uses PORT env variable on Render, defaults to 8081 locally
server.port=${PORT:8081}
server.baseUrl=${BACKEND_URL:http://localhost:8081}

# Active profile
spring.profiles.active=dev


spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d6rt1i7pm1nc73dpr6m0-a.oregon-postgres.render.com/connectsphere_umz3}
spring.datasource.username=${DB_USERNAME:connectsphere_umz3_user}
spring.datasource.password=${DB_PASSWORD:4DbAhok24WKgpNjoKw2VnnLQ4zW2vfH8}
spring.datasource.driver-class-name=${DB_DRIVER:org.postgresql.Driver}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Email — uses env variables on Render
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:chandanaaerupula@gmail.com}
spring.mail.password=${MAIL_PASSWORD:uuqh nnxk pyvp yfya}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cloudinary
cloudinary.cloud.name=${CLOUDINARY_NAME:test}
cloudinary.api.key=${CLOUDINARY_KEY:test}
cloudinary.api.secret=${CLOUDINARY_SECRET:test}

# OAuth2 Google — uses env variables on Render
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

spring.security.oauth2.client.registration.google.scope=email,profile

# OAuth2 GitHub — uses env variables on Render
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=read:user,user:email

# JWT
jwt.secret=${JWT_SECRET:scm2_super_secret_key_for_jwt_token_signing_minimum_256_bits_long}
jwt.expiration=86400000

# Frontend URL — used in OAuth redirect
app.frontend.url=${FRONTEND_URL:https://connectsphere-frontend-txlb.onrender.com}